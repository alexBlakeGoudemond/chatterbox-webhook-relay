spring:
  application:
    name: chatterbox-api-test

api:
  prefix: /api

server:
  port: 8082

logging:
  level:
    root: INFO

common:
  logging:
    appender:
      file:
        location: /var/log/chatterbox-api

packages:
  separate:
    logging:
      level: za.co.psybergate.chatterbox

webhook:
  github:
    secret: secret
    payload: "{\"ping\":\"hello\"}"